import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { ButtonGroup } from '..'

<Meta title='MDX/ButtonGroup' component={ButtonGroup} />

export const SampleButtonGroup = args => <ButtonGroup {...args} />

## ButtonGroup

This Doc will gives you a brief description of the ButtonGroup component.

**Version v1.0.0**

-  All variants in Default | ICON
-  All sizes like Small,Large.
   |

#### **Critical Points**

-  For Icon variant based ButtonGroup component, need to pass selectedOptionId. If we don't pass a selectedOptionId to the component, nothing will be displayed.
